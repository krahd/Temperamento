# Contributing to Temperamento

See [`docs/CONTRIBUTING.md`](docs/CONTRIBUTING.md).

```bash
python -m pip install -e ".[dev]"
make reproducibility
```

Language changes must update the normative specification, implementation, positive and rejection tests, examples, source mapping, and documentation. MuseScore or GUI work must call the shared compiler/API rather than duplicate semantics.
